<!DOCTYPE html>
<html lang="en" data-theme="dim">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Projects - Climate Solutions</title>
    <link href="/css/main.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/navbar', {page: '/solutions/projects'}) %>

    <div class="container mx-auto px-4 py-8">
        <div class="hero min-h-64 bg-base-200 rounded-lg">
            <div class="hero-content text-center">
                <div class="max-w-md">
                    <h1 class="text-5xl font-bold">Projects</h1>
                    <p class="py-6">Explore climate solutions by sector</p>
                    <div class="flex flex-wrap gap-2 justify-center">
                        <a href="/solutions/projects?sector=electricity" class="btn btn-outline btn-sm">Electricity</a>
                        <a href="/solutions/projects?sector=buildings" class="btn btn-outline btn-sm">Buildings</a>
                        <a href="/solutions/projects?sector=food" class="btn btn-outline btn-sm">Food</a>
                        <a href="/solutions/projects" class="btn btn-outline btn-sm">All</a>
                    </div>
                </div>
            </div>
        </div>

        <div class="mt-12">
            <div class="overflow-x-auto">
                <table class="table table-zebra w-full">
                    <thead>
                        <tr>
                            <th>Title</th>
                            <th>Sector</th>
                            <th>Summary</th>
                            <th>Details</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% projects.forEach(project => { %>
                            <tr>
                                <td><%= project.title %></td>
                                <td>
                                <a href="/solutions/projects?sector=<%= project.Sector.sector_name %>" class="btn btn-outline btn-sm">
                                    <%= project.Sector.sector_name %>
                                </a>
                                </td>
                                <td><%= project.summary_short %></td>
                                <td>
                                    <a href="/solutions/projects/<%= project.id %>" class="btn btn-primary btn-sm">
                                        Details
                                    </a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <footer class="footer footer-center p-10 bg-base-200 text-base-content rounded mt-12">
        <div>
            <p>© 2025 Climate Solutions - Making a difference for our planet</p>
        </div>
    </footer>
</body>
</html>