<!DOCTYPE html>
<html lang="en" data-theme="dim">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - Climate Solutions</title>
    <link href="/css/main.css" rel="stylesheet">
</head>

<body>
    <%- include('partials/navbar', {page: '/register' }) %>
        <div class="min-h-screen bg-slate-700 flex items-center justify-center px-4">
            <div class="w-full max-w-md">
                <div class="text-center mb-12">
                    <h1 class="text-5xl font-light text-gray-300 mb-4">Register</h1>
                    <p class="text-gray-400">Please provide your credentials below</p>
                </div>

                <% if(successMessage) { %>
                    <div class="mb-8 p-4 bg-green-900/30 border border-green-800 rounded text-center">
                        <span class="text-green-400 font-medium">
                            <%= successMessage %>
                        </span>
                        <div class="mt-4">
                            <a href="/login"
                                class="bg-green-600 hover:bg-green-500 text-white px-6 py-2 rounded font-medium transition-colors inline-block">Proceed
                                to Log in</a>
                        </div>
                    </div>
                    <% } else { %>

                        <div class="space-y-6">
                            <div>
                                <label class="block text-sm text-gray-500 mb-2">User Name</label>
                                <input type="text" name="userName" form="registerForm"
                                    class="w-full px-4 py-3 bg-slate-600 border border-slate-500 rounded text-gray-200 placeholder-gray-400 focus:outline-none focus:border-blue-400 focus:bg-slate-500"
                                    placeholder="User Name" value="<%= userName %>" required>
                            </div>

                            <div>
                                <label class="block text-sm text-gray-500 mb-2">Password</label>
                                <input type="password" name="password" form="registerForm"
                                    class="w-full px-4 py-3 bg-slate-600 border border-slate-500 rounded text-gray-200 placeholder-gray-400 focus:outline-none focus:border-blue-400 focus:bg-slate-500"
                                    placeholder="Password" required>
                            </div>

                            <div>
                                <label class="block text-sm text-gray-500 mb-2">Confirm Password</label>
                                <input type="password" name="password2" form="registerForm"
                                    class="w-full px-4 py-3 bg-slate-600 border border-slate-500 rounded text-gray-200 placeholder-gray-400 focus:outline-none focus:border-blue-400 focus:bg-slate-500"
                                    placeholder="Confirm Password" required>
                            </div>

                            <div>
                                <label class="block text-sm text-gray-500 mb-2">Email Address</label>
                                <input type="email" name="email" form="registerForm"
                                    class="w-full px-4 py-3 bg-slate-600 border border-slate-500 rounded text-gray-200 placeholder-gray-400 focus:outline-none focus:border-blue-400 focus:bg-slate-500"
                                    placeholder="Email Address" required>
                            </div>

                            <div class="pt-4">
                                <form id="registerForm" method="POST" action="/register">
                                    <button type="submit"
                                        class="bg-green-600 hover:bg-green-500 text-white px-6 py-2 rounded font-medium transition-colors">Register</button>
                                </form>
                            </div>
                        </div>

                        <% } %>

                            <% if(errorMessage) { %>
                                <div class="mt-6 p-4 bg-red-900/30 border border-red-800 rounded">
                                    <span class="text-red-400">
                                        <%= errorMessage %>
                                    </span>
                                </div>
                                <% } %>
            </div>
        </div>
</body>

</html>